@{
    ViewData["Title"] = "Registro exitoso";
}

<div class="container mt-5 text-center">
    <h2 class="text-success">@ViewBag.Mensaje</h2>
    <p class="mt-3">Tu cuenta ha sido creada correctamente.</p>
    <a class="btn btn-primary mt-3" asp-controller="Auth" asp-action="Login">Ir al inicio de sesión</a>
</div>